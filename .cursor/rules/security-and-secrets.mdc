---
alwaysApply: true
---
# Security & Secrets
- Never hardcode secrets. `app.secret_key` must come from `FLASK_SECRET_KEY`.
- Do not commit `DEV_LOG.md`; already in [.gitignore](mdc:.gitignore).
- Logs live under `logs/`; sensitive data should not be printed.
